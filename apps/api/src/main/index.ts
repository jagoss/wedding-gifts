/**
 * Main Entry Point
 *
 * This module is the composition root where all dependencies are assembled
 * and the application is started. Following Clean Architecture, this is
 * the outermost layer that depends on all other layers.
 */

export * from './app';
export * from './container';
